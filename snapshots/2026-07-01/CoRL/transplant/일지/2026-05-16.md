# 📝 2026-05-16 일일 업무 일지

> **[🤖 에이전트 역할 분담 가이드]**
> - **Antigravity (수석 아키텍트):** 압도적인 컨텍스트 창(Context Window)을 활용하여 전체 저장소를 훑고, 모듈 간의 의존성을 파악하며 구현 계획과 아키텍처를 설계한다.
> - **Codex (수석 개발자):** 설계된 아키텍처를 바탕으로 뛰어난 코딩/디버깅 능력을 발휘하여 실제 로직을 빠르고 정확하게 구현한다.

---

## 🤖 1. antigravity (설계 및 분석)

### 🔹 어떤 과정을 진행했는가

- **Reward Shaping 위치 파악:** `risky_overcooked_mdp.py`에서 `BASE_REW_SHAPING_PARAMS`를 확인. 어댑터(`risky_overcooked_env.py`)가 이를 import하여 HSP 학습 보상으로 사용 중임을 재확인.
- **이벤트 감지 메커니즘 이해:** `risky`의 `overcooked_mdp.py` 단일 파일이 ① 이벤트 종류 정의(`EVENT_TYPES`), ② 상황 감지(`resolve_interacts`, `check_can_slip`), ③ 장부 기록(`log_risk_taken`, `log_object_slip` 등) 세 가지를 모두 담당함을 확인.
- **Risky 고유 이벤트 분석:** `drop`, `risked`, `slip`, `handoff` 각 이벤트의 발동 조건 규명. 특히 `empty_slip`은 `check_can_slip`이 빈손일 때 `False`를 반환하므로 **실제로 절대 발생하지 않는 죽은 이벤트**임을 코드로 검증.
- **HSP 구조 이해:** `EVENT_TYPES`(게임 엔진 원시 이벤트)와 `shaped_info_keys`(히든 유틸리티 셀렉션)는 **완전히 별개**임을 확인. 두 파일 위치도 다름 — 전자는 `overcooked_mdp.py`, 후자는 `overcooked_runner.py`.
- **Hidden Utility 확장 계획 수립 (21→31차원):** `risky_multipath` 레이아웃이 onion-only임을 감안해 tomato 계열 이벤트를 전면 제외. 유의미한 10개 이벤트만 선정하여 `implementation_plan.md` 작성.

  | 카테고리 | 추가 이벤트 |
  |---|---|
  | Risking | `onion_risked`, `dish_risked`, `soup_risked` |
  | Slip | `onion_slip`, `dish_slip`, `soup_slip` |
  | Handoff | `onion_handoff`, `dish_handoff`, `soup_handoff` |
  | 빈손 웅덩이 진입 | `empty_risked` (신규 구현 필요) |

- **일지 시스템 정비:** `rule.txt`를 마크다운 기반 템플릿으로 구체화. 두 에이전트의 역할(설계 vs 구현)을 명시.

### 🔹 왜 그 과정이 필요한가

- HSP 원본의 21차원 Hidden Utility는 Risky 환경 고유의 핵심 성향(위험 감수, 협동 패스)을 전혀 포착하지 못함. 이를 반영해야 학습된 에이전트가 Risky 환경에서 의미 있는 다양한 전략을 보여줄 수 있음.
- `empty_slip` 같은 죽은 이벤트를 그대로 두면 학습 중 항상 0인 무의미한 차원이 생김. 이를 `empty_risked`로 대체하면 "빈손으로도 웅덩이를 건너는 성향"이라는 유의미한 정보를 담을 수 있음.
- 코드 수정에 앞서 전체 파이프라인(게임 엔진 → 어댑터 → HSP 러너)의 데이터 흐름을 정확히 파악해야 의도치 않은 버그 없이 안전하게 이식할 수 있음.

### 🔹 기존 환경인 HSP에서는 어떤 것을 사용하거나 수정했는가

- **참조 (수정 없음):**
  - `HSP/hsp/runner/shared/overcooked_runner.py` → `shaped_info_keys` 21개 구조를 기준점으로 삼음.
  - `HSP/hsp/envs/overcooked_new/src/.../overcooked_mdp.py` → 원본 `EVENT_TYPES` 21개 리스트 확인.
- HSP 원본 폴더 내 코드는 **직접 수정하지 않음**. 모든 변경은 어댑터(`transplant/`) 레벨에서만 처리하는 원칙 유지.

### 🔹 기존 환경인 risky에서는 어떤 것을 사용했는가

- **분석 활용:**
  - `risky/src/risky_overcooked_py/mdp/overcooked_mdp.py` → `EVENT_TYPES`, `check_can_slip`, `resolve_enter_water`, `log_risk_taken`, `log_object_slip`, `log_object_handoff` 함수 분석.
- **계획 수립 (아직 미수정):**
  - `empty_slip`을 `empty_risked`로 교체하고, 빈손 웅덩이 진입 감지 로직을 `resolve_enter_water` 내에 추가할 예정.

---

## 💻 2. codex (구현 및 검증)

### 🔹 어떤 과정을 진행했는가

- **Risky 저장소 경로 정리:** 기존 `/home/isl_jhoh/CoRL/overcooked` 폴더명을 `/home/isl_jhoh/CoRL/risky`로 변경. 파일명, 클래스명, import 이름은 유지하고, transplant 쪽의 실제 디렉토리 경로 기본값만 수정.
- **transplant 경로 기본값 수정:** `transplant/bootstrap.py`, `transplant/common.sh`, `transplant/README.md`에서 기본 Risky root를 `/home/isl_jhoh/CoRL/risky` 기준으로 맞춤.
- **Hidden Utility 확장 구현:** 원본 risky 환경은 수정하지 않고, `transplant/adapters/risky_overcooked_env.py`에서 HSP hidden utility를 21개에서 30개로 확장.
- **Risky 고유 이벤트 9개 추가:** `onion_risked`, `dish_risked`, `soup_risked`, `onion_slip`, `dish_slip`, `soup_slip`, `onion_handoff`, `dish_handoff`, `soup_handoff`를 HSP hidden utility 후보로 추가.
- **Risky event → HSP vector 매핑 구현:** Risky MDP의 `event_infos`에서 발생한 이벤트를 `RISKY_EVENT_TO_HSP`, `risky_events_to_hsp()`, `shaped_info_to_array()`를 거쳐 HSP가 읽는 per-agent vector로 변환하도록 정리.
- **HSP weight 차원 수정:** `transplant/common.py`의 hidden utility dimension을 30으로 맞추고, `transplant/scripts/03_train_hsp_s1_risky.sh`의 기본 `w0`, `w1` 길이도 hidden 30개 + sparse reward 1개 구조로 조정.
- **동작 흐름 확인:** `dish_risked`는 agent가 직접 선택하는 action이 아니라, agent가 접시를 들고 물웅덩이에 진입했을 때 Risky MDP가 step 이후 기록하는 event임을 확인. 이 event가 adapter를 통해 HSP hidden utility vector의 `dish_risked` 칸으로 전달되는 흐름을 정리.
- **검증 수행:** `transplant/scripts/00_check_env.sh`, Risky package import 확인, adapter/common py_compile, HSP 학습 스크립트 bash syntax check를 통해 기본 경로와 차원 설정이 맞는지 확인.

### 🔹 왜 그 과정이 필요한가

- 기존 HSP hidden utility 21개는 Risky 환경의 핵심 특성인 물웅덩이 위험 감수, 미끄러짐, handoff 성향을 표현하지 못함.
- Risky 원본 환경을 직접 수정하면 다른 baseline 실험에 영향을 줄 수 있으므로, HSP 이식에 필요한 변환은 transplant adapter 계층에서만 처리하는 것이 안전함.
- `/overcooked`라는 폴더명은 원본 Overcooked benchmark와 혼동될 수 있어, 실제 프로젝트 폴더명을 `/risky`로 바꾸고 import/package 이름은 그대로 유지해 충돌을 줄임.
- HSP의 policy/runner는 fixed-size hidden utility vector를 기대하므로, utility 개수를 늘릴 때 event mapping, vector 생성, weight 기본값 길이를 함께 맞춰야 함.

### 🔹 기존 환경인 HSP에서는 어떤 것을 사용하거나 수정했는가

- **사용:**
  - HSP의 env wrapper/process 구조는 그대로 사용. 병렬 env 실행, reset/step 호출, policy wrapper 연결은 HSP 쪽 구조를 탄다.
- **수정:**
  - HSP 원본 폴더 내부 코드는 직접 수정하지 않음.
  - HSP가 기대하는 hidden utility vector 형식에 맞추기 위해 transplant 쪽 adapter와 config성 기본값만 수정.

### 🔹 기존 환경인 risky에서는 어떤 것을 사용했는가

- **사용:**
  - `risky/src/risky_overcooked_py/mdp/overcooked_env.py`와 `overcooked_mdp.py`의 실제 환경 dynamics, state transition, reward, event 판정 로직을 그대로 사용.
  - `event_infos["dish_risked"][agent_idx] = True`처럼 Risky MDP가 기록한 event 정보를 adapter에서 읽어 HSP feature로 변환.
- **수정 없음:**
  - 원본 risky 환경 파일은 수정하지 않음.
  - `overcooked_env.py`, `overcooked_mdp.py`, `risky_overcooked_env.py` 같은 파일명과 `risky_overcooked_py`, `risky_overcooked_rl` import 이름도 유지.
