# 2026-05-27_2

## Risky multipath subgoal GIF 확인 스크립트 추가

### 배경

`risky_multipath` 계열 layout 중 subgoal `G`가 포함된 버전을 빠르게 눈으로 확인할 필요가 있었다. 기본 `risky_multipath`는 `G`가 없고, subgoal 포함 버전은 별도 layout으로 존재한다.

확인한 layout:

```text
risky_multipath_subgoal
risky_multipath_subgoal_tomato
```

두 layout 모두 `subgoal_disable_steps: 20`을 갖고 있으며, agent가 `G` 타일 위에서 `INTERACT`하면 연결된 puddle `W`들이 일정 step 동안 비활성화됐다가 다시 활성화되는 구조다.

### 수정 내용

`transplant/scripts/render_risky_subgoal_gif.py`를 추가했다.

이 스크립트는 학습 policy 없이 짧은 scripted trajectory만 사용한다.

동작 순서:

1. 지정한 layout을 `OvercookedGridworld.from_layout_name(...)`으로 로드한다.
2. 시작 위치 기준으로 `G`에 인접한 agent를 찾는다.
3. agent를 `G` 위로 이동시키고 `INTERACT`를 실행한다.
4. 이후 `STAY` action을 몇 step 유지하면서 `W`가 사라졌다가 돌아오는 장면을 GIF로 저장한다.

기본 실행:

```bash
/home/isl_jhoh/miniconda3/envs/corl/bin/python transplant/scripts/render_risky_subgoal_gif.py
```

tomato variant 실행:

```bash
/home/isl_jhoh/miniconda3/envs/corl/bin/python transplant/scripts/render_risky_subgoal_gif.py --layout risky_multipath_subgoal_tomato
```

### 생성된 GIF

기본 subgoal layout:

```text
transplant/results/subgoal_render/gifs/risky_multipath_subgoal/subgoal_toggle.gif
```

tomato variant:

```text
transplant/results/subgoal_render/gifs/risky_multipath_subgoal_tomato/subgoal_toggle.gif
```

### 검증

`corl` conda env에서 두 GIF 생성을 확인했다.

기본 subgoal GIF:

```text
size: 274K
frames: 62
resolution: 490x420
```

tomato variant GIF:

```text
size: 305K
frames: 32
resolution: 490x420
```

기본 `python`에는 `imageio`, `pygame`, `numpy`가 없어서 이 스크립트는 기존 transplant 실험과 동일하게 `/home/isl_jhoh/miniconda3/envs/corl/bin/python` 기준으로 실행한다.

### HSP / Risky 관점

HSP algorithm code에서 사용한 것:

- 없음. 이번 작업은 학습 pipeline이나 policy evaluation이 아니라 layout 동작 확인용 render helper다.

HSP env에서 사용하지 않은 것:

- HSP Overcooked env dynamics
- HSP env renderer
- HSP env shaped-info/event 이름

Risky 쪽에서 기준으로 삼은 것:

- `risky_overcooked_py.mdp.overcooked_mdp.OvercookedGridworld`
- `risky_overcooked_py.mdp.overcooked_env.OvercookedEnv`
- `risky_overcooked_py.visualization.state_visualizer.StateVisualizer`
- Risky layout의 `G`, `W`, `subgoal_disable_steps` 동작

transplant에서 담당한 것:

- 확인용 script와 결과 GIF 저장 위치
- 원본 Risky env를 수정하지 않고 subgoal 동작을 재현하는 짧은 trajectory 구성
